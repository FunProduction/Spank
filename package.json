{
  "name": "spark-intimiteit",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host 0.0.0.0",
    "build": "mkdir -p dist && cp index.html dist/index.html",
    "preview": "vite preview --port 3000 --host 0.0.0.0",
    "lint": "echo 'No linting needed'"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
